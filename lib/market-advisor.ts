export function getMarketAdvice(message: string, language: string): string {
  // This file is no longer needed since logic is in the API route
  // Kept for potential future refactoring
  return ""
}
